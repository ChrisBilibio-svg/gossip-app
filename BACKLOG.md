# Fofoca — Build Backlog

Single source of truth for the two autonomous build loops. Each loop picks the **top
unchecked item in its own lane**, ships it, checks it off, and moves on.

## How the loops cooperate (read every iteration)

- **Always `git pull --rebase origin main` before starting.** If there's a conflict, STOP and surface it — never force.
- **Lanes are file-scoped** so the agents rarely touch the same files:
  - 🔵 **Hermes lane (backend / security / scale):** `supabase/migrations/`, `src/lib/`, `scripts/`, `gossip-admin/`, backend tests in `tests/`.
  - 🟣 **Claude Code lane (UI / screens):** `src/screens/`, `src/components/`, `src/theme/`, UI wiring in `App.tsx`.
  - 🟢 **Test & Quality lane:** `tests-ui/` (jest-expo + React Native Testing Library *runtime* tests), `jest.config.cjs`, `babel.config.cjs`, `tests-ui/tsconfig.json`. **Reads** `src/**`/`App.tsx` to test them but **never edits app code**. Run with `npm run test:ui` (+ `npm run typecheck:ui`). One shared touchpoint: testing devDeps + `test:ui`/`typecheck:ui` scripts in `package.json` (the existing `test`/`typecheck` scripts are left untouched). If a component needs a test-only `testID` to be reachable, file a `[handoff]` to the UI lane — don't add it directly.
- **Cross-lane handoffs:** if a backend change needs a UI follow-up (or vice-versa), check your item off and add a paired item to the *other* lane tagged `[handoff]` with a one-line note. Don't reach into the other lane's files yourself.
- **Keep each change small, atomic, typechecked, and tested.** Never commit a broken build (`tsc --noEmit` + `node --test tests/*.test.mjs` must pass). Commit + push each item separately.
- **STOP and ask the human before:** applying a NEW Supabase migration (the human runs those by hand), any destructive/irreversible action, or any product / pricing / legal / brand decision.

---

## 🧭 Roles & current priorities (read first)

Three agents, file-scoped lanes. Who owns what, and what's next:

- 🔵 **Hermes** (Codex 5.5) — **backend · security · scale · monetization/ads.** Files: `supabase/migrations/`, `src/lib/`, `scripts/`, `gossip-admin/`, `.github/`, backend `tests/`. **Next, in order:** #47 sparkline history (migration-backed handoff; human applies DB migration).
- 🟣 **Other Claude** — **UI & screens.** Files: `src/screens/`, `src/components/`, `src/theme/`, `App.tsx`. **Next:** ⭐ **#25 Filter redesign v2 (sort + quick toggles)** — then the backend-blocked UI handoffs (#51 ads placements, #52 avatar persist) as they unblock. (#19/#21–24 all shipped.)
- 🟢 **This Claude** — **Test & Quality (the safety net) + Chris's research/coordination.** Files: `tests-ui/`, jest/babel config, `test:ui`/`typecheck:ui` scripts (reads app code, never edits it). Certifies every lane's work green (tsc + node + UI tests); also handles naming / business / docs for Chris. **Next:** TQ-003 (CI wiring — needs Hermes), TQ-004 (cover void state + redesign + rebrand), and verify #24/#49 after they land.

> ⭐ **Rebrand shipped (Viddi). Current priorities: UI → #25 Filter redesign v2; Hermes → #47 sparkline history + #53 comment_count.**

---

## 🔵 Hermes lane — backend, security, scale

Priority order (top = next):

> ⭐ **Current priorities: #49 (rebrand strings) + #50 (ad integration, Chris-assigned)** — do these before #47/#48.

1. [x] **SEC-001 (HIGH): Fix stored XSS in `gossip-admin/admin.html`.** Escape every user/content-controlled value rendered into `innerHTML`: comment body (`:270`), report reason (`:269`), draft summary (`:122`), source labels. Also fix the `onclick` attribute injection at `:124/:206` (use `data-` attributes + `addEventListener` instead of inline `onclick` with `JSON.stringify`). Use the existing `esc()` helper consistently. _(done Hermes)_
2. [x] **SEC-002: `place_bet` draft guard.** New migration `0014_place_bet_draft_guard.sql` rejects draft rumors inside the `place_bet()` SECURITY DEFINER RPC before status/publish checks. (Human applies the migration.) _(done Hermes)_
3. [x] **SCALE: Denormalize rumor reaction counts.** New migration `0015_denormalize_rumor_reaction_counts.sql`: add `like_count`/`dislike_count` columns to `rumors`, maintain via trigger on `rumor_reactions` (same pattern as `social_reposts`). Update `src/lib/reactions.ts` to read the columns instead of the live `rumor_reaction_summary` aggregate. `[handoff]` none (UI reads same shape). _(done Hermes — human applies migration)_
4. [x] **SCALE: Single feed RPC.** New migration `0016_single_feed_rpc.sql` adds `get_feed(p_limit)` SECURITY DEFINER function returning rumors + caller's choices + caller's reactions in one call; update `src/lib/rumors.ts#fetchFeed` to use it (4 round-trips → 1). `[handoff: UI]` none expected (keep `FeedResult` shape identical). _(done Hermes — human applies migration)_
5. [x] **SCALE: Atomic bet counter.** New migration `0017_atomic_place_bet_counter.sql` removes the `SELECT … FOR UPDATE` hot-row lock in `place_bet`; uses a single atomic `UPDATE … RETURNING` increment so the daily hero rumor doesn't serialize 1k concurrent bets. _(done Hermes — human applies migration)_
6. [x] **SEC: Rate limiting** on comments / reactions / reposts / bets (DB-level throttle or Edge Function) to stop insert spam. New migration `0018_rate_limit_writes.sql` adds DB-level per-user fixed-window throttles for comments, comment likes/reports, rumor reactions, social reposts/reactions, and bets. _(done Hermes — human applies migration)_
7. [x] **OPS: Anonymous-user cleanup** dry-run job only: `scripts/cleanup-anonymous-users.mjs` reports stale anonymous `auth.users` with no app activity; it does not delete users. _(done Hermes — safe dry run only)_
8. [x] **SEARCH: Server-side search RPC** (`ilike` on summary + article over the whole catalog). New migration `0019_server_search_rpc.sql` adds feed-compatible `search_rumors(p_query, p_limit)` with accent-insensitive summary/article matching and newest-first results. `[handoff: UI]` swap `src/lib/feedSearch.ts` call site to the RPC when ready. _(done Hermes — human applies migration)_
9. [x] **CI: Minimal `permissions:`** on GitHub Actions workflows (`contents: read` etc.). Added explicit read-only `contents: read` permissions to ingest and deadline workflows. _(done Hermes)_
10. [x] `[handoff from UI]` **Rank-change data.** New migration `0020_leaderboard_rank_delta.sql` snapshots daily profile ranks and exposes `rankDelta` through `get_leaderboard(p_limit)` so the UI can show ▲/▼ movement on the leaderboard. _(done Hermes — human applies migration; snapshot function should be scheduled/called daily)_
11. [x] **OPS: Daily rank snapshot runner.** Non-migration: added `scripts/snapshot-leaderboard-ranks.mjs` and `.github/workflows/snapshot-leaderboard-ranks.yml` to call `snapshot_leaderboard_ranks()` daily after `0020` is applied, so `rankDelta` has previous-day data. _(done Hermes)_
12. [x] **OPS: Bot health summary.** Non-migration: added read-only `scripts/bot-health-summary.mjs` and `.github/workflows/bot-health-summary.yml` to check recent ingest/deadline/rank activity and print a Telegram/Actions-friendly health report without changing data. _(done Hermes)_
13. [x] **SEC: Secret/config preflight.** Non-migration: added `scripts/config-preflight.mjs` and `.github/workflows/config-preflight.yml` to validate required env names, refuse placeholder Supabase URLs/keys, and redact accidental secret-looking output. _(done Hermes)_
14. [x] **SCALE: Client RPC fallback hardening.** Non-migration: added `src/lib/rpcFallback.ts`; `fetchFeed()` falls back to legacy table reads if `get_feed` is missing, and `getLeaderboard()` falls back to profiles when `get_leaderboard` is missing so UI shapes stay intact while migrations apply. _(done Hermes)_
15. [x] **OPS: Migration drift checker.** Non-migration: added read-only `scripts/check-migration-drift.mjs` to compare local migration filenames with `supabase_migrations.schema_migrations` and report unapplied/unknown versions via `psql`. _(done Hermes)_
16. [x] **SEC: Admin audit log.** Migration: `0021_admin_audit_log.sql` adds append-only `admin_audit_events` table + service-role-only `log_admin_audit_event(...)` function so future admin/curator tooling can record sensitive moderation/curation actions without exposing logs to clients. _(done Hermes — human applies migration)_
17. [x] **MOD: Generic content reports.** Migration: `0022_content_reports.sql` adds `content_reports` for rumor/repost/profile abuse reports beyond comments, with self-insert/read-own policies, curator review policies, status tracking, and duplicate-report prevention. _(done Hermes — human applies migration)_
18. [x] **GROWTH: Notification preferences + devices.** Migration: `0023_notification_preferences.sql` adds per-user notification preferences and Expo/device token registry tables so future push notifications can target opted-in users without storing more personal data than needed. _(done Hermes — human applies migration)_
19. [x] **ANALYTICS: Minimal event telemetry.** Migration: `0024_analytics_events.sql` adds a privacy-minimal `analytics_events` table for opted-in product events plus service-role/admin access only for aggregate analysis; no client-readable analytics rows. _(done Hermes — human applies migration)_
20. [x] **MOD: Unified moderation queue RPC.** Migration: `0025_moderation_queue_rpc.sql` adds a curator-only `get_moderation_queue(p_limit)` RPC that merges unresolved `comment_reports` and open `content_reports` into one newest-first queue for future admin tooling. _(done Hermes — human applies migration)_
21. [x] **SEC: Moderation audit triggers.** Migration: `0026_moderation_audit_triggers.sql` adds triggers that call `log_admin_audit_event(...)` when curators resolve comment reports or update generic content report status, so sensitive moderation actions leave an append-only audit trail. _(done Hermes — human applies migration)_
22. [x] **FEAT: Repost replies (backend).** Migration `0027_social_repost_replies.sql` adds `social_repost_replies` (`repost_id` → `social_reposts` on delete cascade, own-user body 1–280, visible status), RLS (read visible + author-not-blocked, insert own, delete own), denormalized `reply_count` on `social_reposts` via insert/delete triggers, per-user reply insert rate limit, and `reply_count` in `social_repost_feed`. `src/lib/social.ts` now exposes `replyCount`, `getRepostReplies(repostId)`, and `createRepostReply(repostId, body)`. `[handoff: UI]` → Claude builds the reply thread (UI #16). _(done Hermes — human applies migration)_
23. [x] **FEAT: getRumorById (data).** Added `src/lib/rumors.ts#getRumorById(id)` returning a full `Rumor` via an RLS-safe single-row read, mapped through the same caller-state + `mapRumorRows` path as `fetchFeed`, so the UI can open the original rumor from a repost. `[handoff: UI]` → Claude wires tap-to-original (UI #17). _(done Hermes)_
24. [x] **SEARCH: Client server-search helper.** Added `src/lib/feedSearch.ts#searchRumorsByQuery(query, fallbackRumors, limit)` to call the existing `search_rumors` RPC and map feed-compatible rows, while preserving local `filterRumorsByQuery` fallback through `feedSearchCore` when Supabase/RPC is unavailable. `[handoff: UI]` Feed can now swap search from local filtering to this helper without backend changes. _(done Hermes — no new migration)_
25. [x] **PRODUCT: Mandatory 7-day prediction windows.** Migration `0028_seven_day_prediction_deadlines.sql` makes `deadline` the default resolution policy, gives open published rumors a fresh seven-day window, refreshes drafts to publish+7 days on approval, and enforces publish+7 days for future open rumors. Ingest now frames every gathered gossip as a seven-day prediction and admin create/approve paths stamp deadline policy. _(done Hermes — human applies migration; enable deadline resolver only after approval)_
26. [x] **SEC: Env-file leak guard.** Non-migration hardening: `.gitignore` now blocks broad `.env.*` files while explicitly allowlisting `.env.example`, and `scripts/config-preflight.mjs` now checks env-file inventory without printing values so CI/local preflight fails if any non-example env file is tracked or env ignore coverage regresses. _(done Hermes — no migration)_
27. [x] **SEC: Dependency audit gate.** Non-migration hardening: added `scripts/audit-dependencies.mjs`, `npm run audit:deps`, and `.github/workflows/dependency-audit.yml` so CI/local checks fail on high/critical npm vulnerabilities while reporting current moderate Expo/RN advisories without unsafe `npm audit fix --force` SDK churn. _(done Hermes — no migration)_
28. [x] **OPS: Import-safe deadline resolver config.** Non-migration reliability: `scripts/resolve-deadlines.mjs` now parses runtime env only inside the CLI path via `buildDeadlineConfig`, so tests and future orchestration can import helper functions even when CI/local env has invalid resolver values. _(done Hermes — no migration)_
29. [x] **OPS: Import-safe bot health config.** Non-migration reliability: `scripts/bot-health-summary.mjs` now defers `BOT_HEALTH_LOOKBACK_HOURS` parsing until the CLI runs, so tests/future orchestration can import pure helpers even if a local/CI env var is malformed. _(done Hermes — no migration)_
30. [x] **OPS: Import-safe ingest config.** Non-migration reliability: `scripts/ingest.mjs` now defers Anthropic/Supabase/MAX_DRAFTS env parsing into `buildIngestConfig()` inside the CLI path, validates MAX_DRAFTS, and keeps exported helpers import-safe for tests/future orchestration even with malformed local env. _(done Hermes — no migration)_
31. [x] **QUALITY: Node ESM test runtime.** Non-migration quality: `package.json` now declares `"type": "module"` and `tests/node-runtime.test.mjs` locks it in, eliminating Node's `MODULE_TYPELESS_PACKAGE_JSON` reparsing warnings when backend tests import TypeScript helper modules. _(done Hermes — no migration)_
32. [x] **OPS: Import-safe remaining ops scripts.** Non-migration reliability: `scripts/cleanup-anonymous-users.mjs` and `scripts/snapshot-leaderboard-ranks.mjs` now defer env parsing into CLI-only config builders, preserving importable helpers even when local/CI runtime env vars are malformed. _(done Hermes — no migration)_
33. [x] **OPS: Config-threaded bot health runner.** Non-migration reliability: `scripts/bot-health-summary.mjs` now builds a CLI-only config object and threads Supabase URL/service credentials through fetch helpers instead of capturing them in import-time globals, with regression coverage for supplied-env config parsing. _(done Hermes — no migration)_
34. [x] **OPS: Config-threaded migration drift checker.** Non-migration reliability: `scripts/check-migration-drift.mjs` now exposes `buildMigrationDriftConfig(env)`, keeps DB URL selection in the CLI path, and has import-safety coverage for malformed local database env values. _(done Hermes — no migration)_
35. [x] **CI: Push/PR quality gate.** Non-migration CI hardening: added `.github/workflows/quality-gate.yml` plus `npm run config:preflight` so pushes/PRs run `npm ci`, TypeScript, Node tests, env-file leak checks, and the high/critical dependency-audit gate with minimal read-only permissions. _(done Hermes — no migration)_
36. [x] **OPS: Deadline resolver config validation export.** Non-migration reliability: `scripts/resolve-deadlines.mjs` now exports a redaction-safe `validateDeadlineConfig()` helper and has regression coverage proving supplied env parsing does not read malformed ambient `process.env`. _(done Hermes — no migration)_
37. [x] **SEC: Server-side comment guideline guard.** Migration `0029_comment_guideline_insert_guard.sql` replaces the broad comment insert RLS policy so only users with `profiles.accepted_guidelines = true` can insert comments; this closes the UI-only guideline enforcement gap. _(done Hermes — human applies migration)_
38. [x] **SEC: Profile handle validation.** Migration `0030_profile_handle_validation.sql` replaces `set_handle()` so the DB enforces lowercase 3–20 char `[a-z0-9_]` handles, rejects reserved platform/admin names, and keeps execution authenticated-only. _(done Hermes — human applies migration)_
39. [x] **SEC: Dedicated CI secret scan.** Non-migration hardening: added `.github/workflows/secret-scan.yml` to run Gitleaks on pushes and pull requests with read-only repository permissions and no product secrets, plus regression coverage in `tests/github-actions.test.mjs`. _(done Hermes — no migration)_
40. [x] **SEC: Stricter ingest AI output validation.** Non-migration content-safety hardening: `scripts/ingest.mjs` now exports/tests `shouldUseDraft()` and rejects non-boolean `use`, unsafe statuses, too-short summaries, and overlong summaries/articles before any draft insert; payload building also trims accepted model text. _(done Hermes — no migration)_
41. [x] **OPS: Redaction-safe ingest config validation.** Non-migration reliability: `scripts/ingest.mjs` now exports `validateIngestConfig(config)` so orchestration/tests can fail closed on missing Anthropic/Supabase credentials by env name only, without reading ambient `process.env` or leaking supplied secret values. _(done Hermes — no migration)_
42. [x] **OPS: Scheduled workflow concurrency guards.** Non-migration reliability: stateful scheduled/manual workflows (`ingest`, `resolve-deadlines`, and `snapshot-leaderboard-ranks`) now use GitHub Actions `concurrency` with `cancel-in-progress: false`, so overlapping runs queue instead of racing duplicate draft inserts/resolutions/snapshots. _(done Hermes — no migration)_
43. [x] **OPS: Dependency audit CLI fail-closed parsing.** Non-migration reliability: `scripts/audit-dependencies.mjs` now exports/test-covers `parseAuditArgs()` and rejects `--threshold` without an explicit value instead of silently falling back to the default high threshold. _(done Hermes — no migration)_
44. [x] **SEC: Config preflight CLI fail-closed parsing.** Non-migration hardening: `scripts/config-preflight.mjs` now exports/test-covers `parsePreflightArgs()` and rejects unknown or malformed flags such as `--env-file-security-only=true` instead of silently running the wrong preflight mode. _(done Hermes — no migration)_
45. [x] `[handoff from Claude — 0028 hybrid resolution]` **Match the hybrid resolution model in admin + ingest + feed.** Migration `0028_hybrid_resolution_model.sql` was **rewritten** (from the old mandatory-7-day-CAP draft) and is **applied**. It makes resolution per-rumor: `resolution_policy='evidence'` (new default) resolves on credible sources and **VOIDs** (a push — bets returned, **no** points/accuracy hit) if the resolve-by `prediction_deadline` lapses; `resolution_policy='deadline'` keeps CAP-on-deadline for genuine "by date X" questions. Adds `void_rumor(uuid,text)` and a policy-aware `resolve_expired_prediction_deadlines`. Done Hermes: `gossip-admin/admin.html` now defaults manual/create approval to `evidence` + resolve-by and exposes the evidence/deadline toggle; `scripts/ingest.mjs` frames drafts as evidence-policy resolve-by markets; shared feed/client contracts include `status='void'` and the resolved tab treats VOID as closed. Tests updated (`admin-xss`, `ingest`, UI rumor mapping) and green.
46. [x] `[handoff from UI — unblocks UI #19]` **Surface `status='void'` to the client.** Done with #45: added `'void'` to the `RumorStatus` union in `src/lib/rumors.ts`, passes through `mapRumorRows`, and has pure UI mapping coverage. The feed resolved tab now treats VOID as a closed/no-verdict status; UI #19 can render dedicated copy/styling on top. _(done Hermes as part of #45)_
47. [x] `[handoff from UI — prediction-market redesign]` **Surface probability-over-time history.** Done Hermes: migration `0034_rumor_odds_history.sql` adds `rumor_odds_snapshots`, `snapshot_rumor_odds()`, `get_rumor_odds_history(p_rumor_id, p_limit)`, and `odds_history integer[]` on `get_feed`/`search_rumors`; `src/lib/rumors.ts` maps it as `Rumor.oddsHistory` with empty-array fallbacks; `scripts/snapshot-rumor-odds.mjs` + `.github/workflows/snapshot-rumor-odds.yml` snapshot every 3 hours with concurrency guards. **Human still needs to apply `0034` in Supabase before live payloads include odds history.** _(done Hermes)_
55. [ ] `[handoff: UI — sparkline history]` **Use real odds history once 0034 is applied.** Replace `placeholderSparkline(teaPct)` in market cards/detail with `rumor.oddsHistory` when non-empty; keep the existing placeholder fallback for pre-migration / no-snapshot markets. _(blocked until Chris applies `0034_rumor_odds_history.sql` and the scheduled snapshot has run)_
61. [ ] `[handoff: UI — source clustering / backend #58]` **Render source clusters once 0037 is applied.** Add a compact `N fontes` indicator on `MarketCard` and `RumorDetail` using the exact client field `Rumor.sourceCount: number`; use `rumor.evidenceSources: EvidenceSource[]` already surfaced on `Rumor` for the detailed source list (`EvidenceSource = { id: string; sourceUrl: string; sourceLabel: string | null; supportsOutcome: boolean; note: string | null }`). Contract: `sourceCount` is denormalized from `rumors.source_count`, but pre-migration it falls back to `evidenceSources.length`, so the UI can render safely even before live DB apply. Blocked until Chris applies `0037_source_clustering.sql` in Supabase for accurate counts from RPC/direct reads. Paired with backend #58.
62. [ ] `[handoff: UI — keyword follows / backend #59]` **Add “Seguir tópico / 🔔 me avise sobre…” UX once 0038 is applied.** Add an entry point from search/topic/category context to subscribe to a keyword, plus a manage-keywords list in Profile/notifications where users can view and delete subscriptions for LGPD. Final backend contract in `src/lib/notifications.ts`: `normalizeKeyword(keyword: string): string`; `subscribeKeyword(keyword: string): Promise<{ ok: boolean; keyword?: string; error?: string }>`; `unsubscribeKeyword(keyword: string): Promise<{ ok: boolean; keyword?: string; error?: string }>`; `listKeywords(): Promise<{ keywords: Array<{ id: string; keyword: string; createdAt: string }>; error: string | null }>` . Consent/prereq: use these keyword APIs without native deps, but real push delivery also requires push permission + Expo push-token registration into existing `push_devices`, which needs `expo-notifications` and a dev-build rebuild (human-gated). Blocked until Chris applies `0038_keyword_notifications.sql`; push-token registration remains blocked until Chris approves native dep/rebuild. Paired with backend #59.
63. [ ] `[handoff: UI — update markets / backend #60]` **Render follow-up markets once 0039 is applied.** Add a compact “Atualiza mercado”/“continuação” module on `MarketCard` and `RumorDetail` when the exact client field `rumor.updatesRumor` is non-null. Final contract in `src/lib/rumors.ts`: `Rumor.updatesRumor: { id: string; summary: string } | null`; the value is `null` before migration/no parent, and populated from `updates_rumor_id` + `updates_rumor_summary` after `0039_update_markets.sql`. Recommended UX: show a subtle linked parent pill/card with `updatesRumor.summary`; tapping it should open `getRumorById(updatesRumor.id)`/existing detail navigation. Blocked until Chris applies `0039_update_markets.sql` in Supabase. Paired with backend #60.
48. [x] `[handoff from UI — prediction-market redesign]` **Add a `category` field to rumors.** Done Hermes: added migration `0031_rumor_categories.sql` with nullable bounded `rumors.category`, category index, feed/search RPC payload updates, and search-by-category support; `Rumor`/`RumorRow` + `mapRumorRows` now surface `category`; local feed search searches category labels; ingest infers topic labels (`Celebridades`, `BBB`, `Futebol`, `Música`, `Novelas`, `Influencers`) or accepts Claude-provided category; curator admin can set/display categories. Regression coverage added in node + UI tests. Chris applied the migration in Supabase, so live RPC payloads can include `category`. _(done Hermes)_
49. [x] `[handoff from UI — rebrand #24]` **REBRAND backend strings: "Fofoca" → "Viddi".** Chris locked the name **Viddi** (see UI #24 / [[Gossip App — Name Decision (Viddi)]]). Done Hermes: `gossip-admin/admin.html` now brands the curator console as Viddi and uses market copy; `scripts/ingest.mjs` prompts as Viddi; `scripts/bot-health-summary.mjs` reports Viddi bot health; `src/lib/bets.ts` fallback copy no longer says fofoca; `src/lib/supportContact.ts` uses `contato@viddi.app.br` and Viddi mailto copy. Infra unchanged: repo/Supabase and curator login email (`admin@fofoca.com`) stay as-is unless Chris says otherwise. Tests updated. _(done Hermes)_
50. [x] `[Chris-assigned]` **MONETIZATION: Ad integration (research + integrate).** Done Hermes research + safe scaffold: `docs/monetization-ads.md` explains how ads work, how Viddi gets paid, AdMob setup/payment flow, LGPD/privacy guardrails, recommended placements, and the implementation checklist. `src/lib/monetization.ts` centralizes ad policy: feed native card, post-resolution interstitial, future rewarded optional unit, consent gating, frequency caps, personalized/non-personalized decision, and **Pro = ad-free**. Tests cover the guardrails. Native SDK install is intentionally deferred until Chris creates real AdMob app IDs/ad unit IDs; next handoff is to add `react-native-google-mobile-ads` + `expo-tracking-transparency` and UI placements with test IDs. _(done Hermes scaffold; blocked on AdMob IDs for live integration)_

51. [ ] `[handoff: UI + Hermes after Chris creates AdMob IDs]` **Install/render AdMob with test IDs, then swap to real Viddi IDs.** Use `react-native-google-mobile-ads` + `expo-tracking-transparency`; wire `src/lib/monetization.ts` into feed native card and post-resolution interstitial. Add consent UX before personalized ads. Keep Pro ad-free and never show ads before vote/payoff/truth reveal. Requires native rebuild. _(blocked: needs AdMob app IDs/ad unit IDs + privacy copy sign-off)_
52. [x] `[handoff from UI — avatars #23]` **Persist user avatar server-side.** Done Hermes: migration `0033_profile_avatar.sql` adds nullable `profiles.avatar` with curated emoji validation and authenticated-only `set_avatar(p_avatar)` RPC; `get_leaderboard`, comment profile lookups, `social_repost_feed`, repost replies, and `src/lib/profile.ts#setAvatar()` now surface/write nullable avatars with pre-migration fallbacks (`avatar: null`). **Human still needs to apply the migration in Supabase before live avatars appear to others.** _(done Hermes)_
54. [ ] `[handoff: UI — avatars]` **Render server avatars across personas once 0033 is applied.** Use `src/lib/profile.ts#setAvatar(avatar)` from the picker, keep/retire the local AsyncStorage value as a migration fallback, and render `avatar` on Profile/leaderboard/comments/social/repost replies instead of first-letter/default fallbacks. _(blocked until Chris applies `0033_profile_avatar.sql`)_
53. [x] `[handoff from UI — filter redesign #25]` **Expose `comment_count` on the feed payload.** Done Hermes: migration `0032_rumor_comment_counts.sql` adds denormalized visible `rumors.comment_count`, backfills from `comments.status='visible'`, maintains counts on comment insert/status update/delete, and exposes `comment_count` through `get_feed`/`search_rumors`; `Rumor`/`RumorRow` + `mapRumorRows` surface `commentCount`, direct-table reads retry without the new column until the migration is applied, and `filterRumors(..., 'most-commented')` sorts by comment count then recency. **Human still needs to apply the migration in Supabase before live payloads include `comment_count`.** _(done Hermes)_
56. [x] `[Chris-reported bug]` **Fix delete account failure.** Done Hermes: migration `0035_delete_account_social_cleanup.sql` replaces `delete_my_account()` so it deletes post-v1 user-owned rows (`social_repost_replies`, `social_repost_reactions`, `social_reposts`, `rumor_reactions`, `content_reports`, notification/device rows, analytics events, rate-limit state) before removing `auth.users`; `ProfileScreen` now checks the RPC result, shows an error, and does not navigate as deleted on failure. **Human still needs to apply the migration in Supabase before live account deletion is fixed.** _(done Hermes)_
57. [x] `[Chris cybersecurity push]` **Full rate-limit/input-validation/secret hardening pass.** Done Hermes: migration `0036_security_input_rate_limits.sql` adds remaining DB write throttles, safe-text/size constraints, and rate-limited `set_handle`/`set_avatar`; client auth routes enforce 5 failed attempts per 15 minutes; `src/lib/inputValidation.ts` centralizes user-text/UUID/email/password validation for comments/social writes; admin Supabase project config was moved out of committed HTML into runtime config; `SECURITY_REVIEW.md` records the scan and remaining vulnerabilities. **Human still needs to apply `0036` in Supabase.** _(done Hermes)_
58. [x] `[Chris content/notifications push — 1/3]` **Source clustering: multiple sources on one prediction.** Done Hermes: migration `0037_source_clustering.sql` adds `rumors.source_count` + `rumors.event_key`, a trigger-maintained source-count backfill, unique per-rumor evidence URLs, and feed/search RPC payload support; `scripts/ingest.mjs` computes a conservative normalized event key and attaches same-event articles as `rumor_evidence_sources` instead of creating duplicates; `src/lib/rumors.ts` maps `Rumor.sourceCount: number` with pre-migration fallback to `evidenceSources.length`; `gossip-admin/admin.html` displays `N fontes` and adds manual split/merge cluster controls. **Human still needs to apply `0037_source_clustering.sql` in Supabase before live RPC payloads include `source_count`/`event_key`.** _(done Hermes)_
59. [x] `[Chris content/notifications push — 2/3]` **Keyword follow + notifications.** Done Hermes: migration `0038_keyword_notifications.sql` adds LGPD-minimal `keyword_subscriptions(user_id, keyword, created_at)` with own-row RLS, normalized accent/case-insensitive keywords, and a 20-keyword/user cap; publish triggers enqueue `notification_queue` rows for opted-in subscribers while respecting `notification_preferences.breaking_news` and a 3/hour frequency cap; `src/lib/notifications.ts` exposes `normalizeKeyword`, `subscribeKeyword`, `unsubscribeKeyword`, and `listKeywords`; `scripts/send-keyword-notifications.mjs` + `.github/workflows/send-keyword-notifications.yml` deliver queued notifications through Expo Push every 15 minutes using existing `push_devices`. **Human still needs to apply `0038_keyword_notifications.sql`; client push-token registration via `expo-notifications` remains native-dep/dev-build human-gated.** _(done Hermes)_
60. [x] `[Chris content/notifications push — 3/3]` **Update markets quoting prior news.** Done Hermes: migration `0039_update_markets.sql` adds nullable `rumors.updates_rumor_id` referencing `rumors(id)` with self-link guard + index and extends feed/search RPCs with `updates_rumor_id`/`updates_rumor_summary`; `scripts/ingest.mjs` links follow-up markets only on conservative same-entity/changed-claim matches (dedupe/source clustering still wins for same-event matches); `src/lib/rumors.ts` exposes `Rumor.updatesRumor: { id, summary } | null` with pre-migration fallback; admin can manually set/clear the update parent. **Human still needs to apply `0039_update_markets.sql` in Supabase before live RPC payloads include update references.** _(done Hermes)_
64. [x] `[Chris scoring/stats push]` **First-class accuracy + prediction streak stats.** Done Hermes: migration `0040_profile_streaks.sql` adds `profiles.current_streak` + `profiles.best_streak`, rewrites `resolve_rumor()` so correct predictions increment the running streak and wrong resolved predictions reset it, restates `void_rumor()` as a push that does not update profiles, and backfills every profile from scored prediction history in chronological resolution order while ignoring `is_correct is null` VOID rows. `src/lib/profile.ts` now exposes `MyProfile.currentStreak` + `bestStreak` beside existing `correctCount`/`resolvedCount`, and `src/lib/leaderboard.ts` exposes `LeaderRow.currentStreak`, all with pre-migration zero fallbacks. **Chris applied `0040_profile_streaks.sql`; `recompute_profile_streaks()` returned `14`, so live streak values are populated/backfilled.** _(done Hermes)_
65. [x] `[Chris O Profeta groups push]` **Private time-limited Grupos backend.** Done Hermes: migration `0041_groups_leagues.sql` adds `groups` + `group_members` with member-only RLS, invite-code format/unique index, duration validation (future, >=1 day, <=1 year), 50-members/group and 20-groups/user caps, SECURITY DEFINER RPCs (`create_group`, `join_group`, `leave_group`, `get_my_groups`, `get_group`, `get_group_leaderboard`, `rename_group`, `remove_group_member`, `delete_group`, `regenerate_group_invite`) with explicit `auth.uid()`/handle/rate-limit checks, collision-retry invite regeneration, and owner-transfer-on-leave. Group leaderboard scoring sums `predictions.points_awarded` for predictions awarded/resolved inside `[starts_at, min(ends_at, now()))`, freezes ended groups, excludes VOID (`is_correct is null`), and orders by points/correct/resolved/joined_at/id. Migration adds/backfills `predictions.awarded_at` from `scored_at`/`rumors.resolved_at` and restates `resolve_rumor()` to stamp it going forward. MVP counts from `starts_at` regardless of join date. `src/lib/groups.ts` exposes typed RPC wrappers with missing-RPC-safe empty results/unavailable flags. **Human still needs to apply `0041_groups_leagues.sql` in Supabase before live groups work.** _(done Hermes)_

---

## 🟣 Claude Code lane — UI & screens

Priority order (top = next):

> ⭐ **DO FIRST: #19 (VOID render).** Then #21–23. (#24 rebrand→Viddi done; checked items #1–20, #24 are history.)

1. [x] **Social screen states.** `SocialScreen.tsx`: proper loading skeletons, empty state ("Seja o primeiro a repostar"), and error state — match the Feed's look. _(done 353daf3)_
2. [x] **Reaction micro-interactions.** `ReactionButtons.tsx`: tap feedback (scale/opacity) + `expo-haptics` on like/dislike; clear active state. _(done 980e3af)_
3. [x] **First-run explainer.** A lightweight one-time overlay teaching 🍵 TEA / 🧢 CAP, resolution, and points — shown before the feed on first launch. _(done e5103cb)_
4. [x] **Consistent empty/error states** across `MyBetsScreen`, `LeaderboardScreen`, `ProfileScreen` (shared component in `src/components/`). _(done 9e345a2 — shared EmptyState in MyBets + Leaderboard; Profile has no list zero-state)_
5. [x] **Leaderboard polish.** Podium for top 3, clearer "(você)" highlight, subtle rank-change affordance. _(done 44f7ab6 — podium + você pill; real rank-change needs prev-rank data, handed to Hermes)_
6. [x] **Profile polish.** Visual stat cards (accuracy ring / bars), nicer anonymous avatar. _(done 9ab1de8 — 🍵 avatar + accuracy progress bar)_
7. [x] **Accessibility pass.** `accessibilityLabel`/`Role` on all pressables, 44px min hit targets, contrast check against `tokens.ts`. _(done 1a36fb2 — roles/labels on tabs, cards, account button; reactions/nav/back already had them)_
8. [x] **Article view typography.** Tune `RumorDetail` article spacing/line-height; ensure "Ler mais" affordance is obvious. _(done 180c948 — lineHeight 26, divider, pill read-more)_
9. [x] **Pro paywall scaffold (UI only, non-functional).** A `ProScreen`/sheet showing the Pro value props (early alerts, stats, cosmetics) — no billing wiring yet. _(done 3143428 — ProSheet + "Seja Pro" on Profile; price/copy placeholder, needs sign-off)_
10. [x] **Brand consistency.** Align the Android adaptive-icon background (`app.json` `#E6F4FE`) and splash with the Fofoca pink/`tokens.ts` palette. `[handoff: human]` confirm before changing app icon. _(done 322725d — crystal-ball+tea-cup icon installed; full-bleed + adaptive foreground + pink #FD4388 bg; splash still optional)_
11. [x] **Breaking-news hero treatment.** Give the hero "Fofoca do Dia" card a Choquei-style urgency look: a 🚨/"AGORA" badge, bolder headline, stronger pink accent — make it read as a live event, not a list row. (UI-only in `RumorCard.tsx`/`FeedScreen.tsx`.) _(done 33a7b03 — 🚨 badge + pink-tinted hero card)_
12. [x] **Shareable win card.** When a user's call resolves correctly, render a share-image card ("Eu acertei! 🍵 +X pts") sized for Instagram Stories, with a share action. Drives a viral loop (how DeuxMoi/Choquei content travels). UI-first; may need a `[handoff]` for a deep-link later. _(done 6d8ba5b — text/link Share on PayoffOverlay; Story-image export deferred, see #15)_
13. [x] **Status-tier ladder.** Map points to named tiers (Aprendiz → Fofoqueiro(a) → Vidente → Profeta) shown on Profile + leaderboard. Retention via visible progression (Duolingo-leagues pattern), on-brand with "O Profeta". UI-only (derive tier from existing points). _(done b132b80 — Tier component; badge+progress on Profile, emoji on leaderboard rows)_
14. [x] **Rank-change arrows.** Now that `get_leaderboard` exposes `rankDelta` (migration 0020), show ▲/▼/– movement next to each leaderboard row. UI-only once `leaderboard.ts` surfaces the field. _(done 4564ad1 — RankDelta ▲/▼/– on leaderboard rows)_
15. [x] `[handoff: human]` **Story-image win card.** Render the win as an actual Instagram-Stories image (big 🍵, "EU ACERTEI", +X pts, @handle, fofoca.app watermark) and share the image. Needs `react-native-view-shot` → a dev-build rebuild, so requires human OK before adding the native dep. _(follow-up to win-share 6d8ba5b)_ _(done c1fa7ec — off-screen StoryCard captured via react-native-view-shot, shared via expo-sharing; added both native deps + expo-sharing config plugin. **NEEDS `npx expo run:android` rebuild to work on device.**)_
16. [x] **FEAT: Repost reply thread (UI).** Twitter-style: tapping a repost in `SocialScreen` opens a repost-detail view — the repost on top + a reply thread below + an input to reply. `replyCount` shown on the repost card. _(done 9ab84b5 — new RepostDetail modal)_
17. [x] **FEAT: Tap reposted gossip → original (UI).** Twitter quote-tweet behavior: tapping the embedded rumor box inside a repost opens that rumor's `RumorDetail` (full rumor via `getRumorById`). _(done 9ab84b5)_
18. [x] **UI: Skeleton loaders on the remaining screens.** _(done — shared `src/components/Skeleton.tsx`: one synced shimmer pulse + reduce-motion gate; `SkeletonMarketCard` on Feed; screen-shaped skeletons replace the spinners on Palpites/O Profeta/Profile and the text loader on Social.)_ _(requested by Chris; routed here from the Test & Quality lane because it's app-code UI work.)_ **Current state:** `FeedScreen` (`SkeletonCard` at `src/screens/FeedScreen.tsx:49`, with a `hero` variant) and `SocialScreen` (`SkeletonCard` at `src/screens/SocialScreen.tsx:180`) already render skeletons while `loading`, but each defines its **own duplicated, static** skeleton; `ProfileScreen`, `LeaderboardScreen`, and `MyBetsScreen` still show a bare `<ActivityIndicator>` spinner (`*Screen.tsx`, the `if (loading)` blocks). **Do:** (a) extract a shared `src/components/Skeleton.tsx` — a reusable `<Skeleton>` block + a `<SkeletonCard>` — with a subtle pulse/shimmer via `Animated` (gate on reduce-motion / `AccessibilityInfo.isReduceMotionEnabled`), colored from `tokens.ts` (`surfaceSunken`/`border`). (b) Replace the three spinners with screen-shaped skeletons (leaderboard rows, bet rows, profile stat cards). (c) Refactor Feed + Social to consume the shared component and delete the duplicated local copies. UI-only — every screen already has its `loading` state. `[handoff: Test & Quality]` → once shipped, the 🟢 lane adds render tests asserting each screen shows skeletons while `loading` and real content after (tracked as TQ-004).
19. [x] `[handoff from Claude — 0028 hybrid resolution]` **Render the VOID (push) resolution state.** A rumor can close with **no verdict** (`status = 'void'`) — bets returned, no points/accuracy change. Shown distinctly from confirmed/debunked: `marketView.toMarketStatus`→'VOID' + neutral StatusChip; `RumorCard` "Anulado" tag; `ResolvedSplit` "Anulado (VOID)"; `RumorDetail` neutral resolved banner "↔ Anulado (VOID)"; `MyBetsScreen` resolved void row → neutral, "devolvido", "Anulado — sem veredito · palpite devolvido" (not a loss, not pressable for payoff); feed Resolvidas tab + VOID filter treat it as closed. _(done — typecheck + 131 node + 74 UI tests green)_
20. [x] **Prediction-market redesign (full native re-skin).** Re-implemented the whole UI to match the dark fintech mockup (Chris-supplied): new `tokens.ts` dark+light schemes + `ThemeProvider`/`useTheme` with a persisted toggle, Inter + JetBrains Mono fonts, `@expo/vector-icons`. New market primitives (`StatusChip`, `OddsBar`, `Sparkline`, `MarketCard`, `marketView`); every screen + overlay rebuilt (Feed/Markets, MarketDetail, Palpites, O Profeta, Profile, Social, BottomNav, FirstRun, Payoff settlement) and all shared components migrated (VoteBlock, ReactionButtons, ResolvedSplit, CommentSection, RepostDetail, ProSheet, TermsGate, HandlePrompt, AccountSheet). Real Supabase data flow preserved; win-share image (react-native-view-shot) preserved. Charts approximated with Views (no native dep). `[handoff: backend]` → #47 (sparkline history) + #48 (category). _(done — typecheck + 131 node + 66 UI tests green; design brief in `design/UI_REDESIGN_BRIEF.md`)_
21. [x] **Feed filter.** `FeedScreen` now has a filter panel (sliders icon) with per-tab options — open: Recentes/Encerrando/Populares/Meus palpites/Evidência/Prazo; resolved: Recentes/Populares/Meus palpites/TEA/CAP/VOID — plus the Em aberto/Resolvidas segmented control, wired through `filterRumors`. Category/topic filtering remains deferred to the backend `category` field (`[handoff: backend]` — see #48). _(done — verified typecheck + 131 node + 74 UI tests green)_
22. [x] **Help / Contact section.** New `HelpSheet` (opened from Profile → "Como funciona"): a how-it-works FAQ covering 🍵 TEA / 🧢 CAP, posição trancada, resolution (fonte / prazo / ↔ VOID), pontos & track record, and the "opinião, não acusação" rule. Pairs with the existing Profile → "Contato & Suporte" mailto entry. _(done — typecheck + 131 node + 74 UI tests green)_
23. [x] **Customizable, prominent icons & avatars.** New `AvatarPicker` + curated on-theme emoji set (`src/components/avatar.ts`): the Profile avatar is now a tappable, foreground element with an edit badge; tapping opens a grid to pick an avatar, persisted on-device (AsyncStorage), framed "grátis pra todo mundo — nada de pay-to-win". Status chips/tiers/reactions already carry bold iconography from the redesign. `[handoff: backend]` → a `profiles.avatar` column so the choice shows to OTHER users (leaderboard/comments/social) — see Hermes #51. _(done — typecheck + 131 node + 74 UI tests green)_
24. [x] **REBRAND: "Fofoca" → "Viddi" (app-facing).** Chris locked the final name **Viddi** (2026-06-11; see [[Gossip App — Name Decision (Viddi)]]). Replace the working name "Fofoca" with **Viddi** everywhere the **user** sees it. _(done — app.json display name; FeedScreen wordmark "Viddi."; FirstRunOverlay title+brand; MarketCard + RumorCard "VIDDI DO DIA" hero label; ProfileScreen + ProSheet "Viddi Pro"/"VIDDI PRO"; RumorDetail "Repostado no Viddi Social"; PayoffOverlay share text + StoryCard handle fallback + watermark `fofoca.app`→`viddi.app.br`; terms.ts intro. Kept the common noun "fofoca"=gossip in copy, and left slug/scheme/repo/Supabase/curator-email infra untouched. Confirmed with Chris over backlog-vs-chat name conflict — Viddi is final. typecheck + 131 node + 74 UI tests green.)_ UI-lane files: `app.json` (display `name` → "Viddi"; be cautious changing `slug`/`scheme` — can break EAS/deep links, change deliberately or leave), `src/screens/` (FeedScreen, LeaderboardScreen, ProfileScreen), `src/components/` (FirstRunOverlay, MarketCard, PayoffOverlay, ProSheet, ReactionButtons, RumorCard, RumorDetail), `src/content/terms.ts`. Specific strings: the "**Fofoca do Dia**" hero label, the "**Fofoca Social**" tab, PayoffOverlay's share text ("Eu acertei no Fofoca!" → "…no Viddi!") and the StoryCard watermark (`fofoca.app` → `viddi.app.br`). Keep the crystal-ball+tea icon (no wordmark in it); splash/wordmark may want the new name (optional follow-up). Do **not** rename the GitHub repo, Supabase refs, or curator email infra — product brand only. `[handoff: backend]` → see Hermes #49. _(awaiting pickup)_
25. [x] ⭐ **Filter redesign v2 — sort + quick toggles (Chris-approved).** Refinement of the shipped #21 filters (Chris reviewed and disliked the single jammed chip list + the policy jargon). In `FeedScreen.tsx` + `src/lib/feedSearchCore.ts`, split the one chip row into TWO distinct controls:
    **(A) Ordenar** — single-select, default `🔥 Em alta`: `🔥 Em alta` (blend volume `trueTotal+falseTotal` + recency) · `🆕 Recentes` (createdAt desc) · `⏰ Encerrando logo` (open, nearest `predictionDeadline`) · `💬 Mais comentados` (**gated on Hermes #53** `comment_count`; omit this chip until the field exists).
    **(B) Filtros rápidos** — multi-select toggles, combinable with the sort: `⚖️ Tá empatado` (TEA share 40–60% **and** volume ≥ ~10 so brand-new 0/0 markets don't qualify) · `🆕 Ainda não palpitei` (open tab, `myChoice == null`) · `💬 Com discussão` (`commentCount > 0`; replaces the redundant `🎯 Meus palpites`, because Palpites already has its own tab).
    **REMOVE the `evidence` / `deadline` (Evidência/Prazo) chips** from the UI — resolution policy is a curator mechanic, not a user browse axis.
    **Resolved tab:** sort `🆕 Recentes` / `🔥 Populares` + outcome filter with plain-PT labels `Todos` · `🍵 Deu TEA` (confirmed) · `🧢 Deu CAP` (debunked) · `⚪ Anulado` (void — rename from "VOID") + `🏆 Meus acertos` (resolved where `myChoice` matched: confirmed+true / debunked+false).
    **DEFER the Categoria browse rail to POST-LAUNCH** per Chris — do **not** add category filtering now, even though `category` exists (0031). `[handoff: Test & Quality]` → add sort/filter combination coverage once shipped. _(filed by Test & Quality for Chris, 2026-06-13)_ _(done — `feedSearchCore.ts` adds `applyOpenFeed`/`applyResolvedFeed` + `FeedSort`/`OpenQuickFilter`/`ResolvedSort`/`ResolvedOutcome` (legacy `filterRumors` kept for the server-search fallback); FeedScreen now shows two controls — **Ordenar** (🔥 Em alta default / 🆕 Recentes / ⏰ Encerrando logo) and multi-select **Filtros rápidos** (⚖️ Tá empatado [40–60% & vol≥10] / 🆕 Ainda não palpitei / 💬 Com discussão [commentCount > 0]); Evidência/Prazo chips removed; resolved tab = 🆕 Recentes/🔥 Populares sort + Todos/🍵 Deu TEA/🧢 Deu CAP/⚪ Anulado/🏆 Meus acertos. 💬 Mais comentados omitted pending Hermes #53; Categoria deferred. typecheck + 131 node + 81 UI tests green.)_
26. [x] **UI for Hermes's content/notification backend (migrations 0033–0039).** Wired the shipped backend contracts into the UI: (a) **real sparkline** — MarketCard + RumorDetail use `rumor.oddsHistory`, placeholder only when empty (#47); (b) **"· N fontes"** on MarketCard + RumorDetail when `sourceCount > 1` (#54 source clustering); (c) **update markets (0039)** — "🆕 ATUALIZAÇÃO" chip on the card + a tappable "Atualização de…" quote-box in RumorDetail opening the prior market via `getRumorById` (new `onOpenRumor` prop, wired from Feed + Social); (d) **avatars (#52)** — Profile picker now persists via the server `setAvatar` RPC (local cache kept) and avatars render on Leaderboard/Comments/Social/RepostDetail; (e) **keyword follows** — new `KeywordsSheet` (Profile → "Tópicos que sigo") for list/add/remove via `notifications.ts`. _(done — typecheck + typecheck:ui + 162 node + 83 UI tests green; commits a9ca673 / d394e3d / 57cc448)_ `[handoff: human]` on-device push-token registration (expo-notifications) is still a native dep + dev-build rebuild before keyword notifications deliver on a phone.
27. [ ] `[handoff: UI — streak stats / backend #64]` **Surface prediction streak beside accuracy.** `0040_profile_streaks.sql` is now applied live, so this is unblocked. Profile should keep the existing accuracy/acertos stat derived from `MyProfile.correctCount / MyProfile.resolvedCount` and add a visible `🔥 streak` stat using exact field names `MyProfile.currentStreak` and `MyProfile.bestStreak`; Chris specifically suggested using the third stat slot that currently shows “resolvidos” for the current streak while preserving accuracy. Optional leaderboard flex: show a subtle 🔥 indicator from `LeaderRow.currentStreak`. Paired with Hermes backend #64.
28. [x] `[handoff: UI — Grupos / backend #65]` **Build private time-limited Grupos in O Profeta once 0041 is applied.** _(done — `src/components/groupView.ts` (duration presets/labels/emoji), `GroupsTab.tsx` (list of active+ended groups w/ "encerra em", create sheet w/ duration picker incl. Personalizado 1–365d, join-by-code, handle-gated via `HandlePrompt`), `GroupDetail.tsx` (per-group leaderboard reusing the O Profeta row shape, winner banner when ended, WhatsApp invite share, owner rename/remove/regenerate/delete + leave/ownership-transfer), plus a 🌎 Geral / 👯 Grupos segment in `LeaderboardScreen`. rpcFallback-safe: shows "Grupos chegando em breve" until `0041` is applied live. typecheck green; web preview confirmed the tab + empty state render.)_ Add an O Profeta Grupos view that uses `src/lib/groups.ts` exact wrappers/types: `getMyGroups(): { groups: GroupSummary[] }`, `createGroup(name, endsAt, emoji?)`, `joinGroup(inviteCode)`, `leaveGroup(groupId)`, `getGroup(groupId): GroupMeta`, `getGroupLeaderboard(groupId, limit?): GroupLeaderboardRow[]`, owner actions `renameGroup`, `removeGroupMember`, `deleteGroup`, `regenerateGroupInvite`. UX: list active+ended groups with “encerra em”/ended state and winner banner when ended; create sheet with a **duration picker** (1 sem / 1 mês / 3 meses / 6 meses / custom) passing `p_ends_at`; join-by-code field (6-char uppercase, no 0/O/1/I/L); per-group leaderboard reusing the O Profeta leaderboard row shape (`id`, `handle`, `avatar`, `points`, `correctCount`, `resolvedCount`, `rank`); WhatsApp invite share using `GroupMeta.inviteCode`; owner management for rename/remove/regenerate/delete; reuse `HandlePrompt` before create/join if no handle. Backend fairness note: `ends_at` is intentionally not editable; MVP group score counts predictions from `starts_at` regardless of join date. Blocked until Chris applies `0041_groups_leagues.sql`.

---

## 🟢 Test & Quality lane — runtime test safety net

Priority order (top = next):

1. [x] **TQ-001: UI test harness.** jest-expo + @testing-library/react-native (RNTL 14, React 19 async `render`), `babel.config.cjs` + `jest.config.cjs` (scoped to `tests-ui/`, so the `node --test` backend suite is untouched), global AsyncStorage mock + benign-`act()`-warning filter in `tests-ui/jest.setup.cjs`, and `npm run test:ui` / `npm run typecheck:ui` scripts (`tests-ui/tsconfig.json` for jest globals; root `tsconfig.json` excludes `tests-ui` so the shared typecheck stays app-focused). _(done — 66 tests / 12 suites green)_
2. [x] **TQ-002: Core logic + component coverage.** Runtime tests for the pieces that are scary to break silently: crowd-split math + row mapping (`rumors`), write-once bet translation (`predictions.placeBet`), leaderboard RPC→camelCase mapping, terms-version consent gate, feed search (accent/case-insensitive + newest-first), social/reaction formatting, the status-tier ladder, and rendered/interactive components (`VoteBlock` bet flow, `ReactionButtons`, `BottomNav`, `EmptyState`, `TierBadge`). _(done)_
3. [x] `[handoff: Hermes]` **TQ-003: Wire `test:ui` into CI.** Done Hermes: `.github/workflows/quality-gate.yml` now runs `npm run typecheck:ui` and `npm run test:ui` after backend TypeScript/Node tests, and `tests/ci-quality-gate.test.mjs` locks the workflow coverage. _(done Hermes)_
4. [ ] **TQ-004: Widen coverage** as features land — `CommentSection`, `RepostDetail`, `PayoffOverlay`, `FirstRunOverlay`, `TermsGate`, and screen-level data states (loading/empty/error). Add a test alongside each new UI/lib backlog item.

---

_Reorder items within a lane to change priority. Add new items at the right priority slot._
